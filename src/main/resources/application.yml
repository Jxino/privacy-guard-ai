spring:
  application:
    name: market-pulse
  datasource:
    url: jdbc:mysql://localhost:3306/market-pulse_db
    username: portfolio
    password: portfolio
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
